import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.influenceur',
  appName: 'influenceur',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
