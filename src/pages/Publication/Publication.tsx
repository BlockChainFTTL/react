import { IonContent } from "@ionic/react";

const Publication: React.FC = (props) => {
    const id = props;
    return (
        <IonContent>
        <h1></h1>
        </IonContent>
    );
}

export default Publication;